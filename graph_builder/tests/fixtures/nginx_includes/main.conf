worker_processes auto;

http {
    lua_package_path "/srv/app/lua/?.lua;;";

    lua_shared_dict main_cache 10m;

    include conf.d/*.conf;

    server {
        listen 8080;

        location /health {
            content_by_lua_block {
                ngx.say("ok")
            }
        }
    }
}
